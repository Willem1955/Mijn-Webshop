<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <title>Dashboard – Mijn Webshop</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 1000px;
      margin: 40px auto;
      background: #f9f9f9;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    h1 {
      text-align: center;
      margin-bottom: 30px;
      color: #007bff;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: white;
      border-radius: 8px;
      overflow: hidden;
    }

    th, td {
      padding: 10px 12px;
      border-bottom: 1px solid #ddd;
      text-align: left;
    }

    th {
      background: #007bff;
      color: white;
    }

    tr:hover {
      background: #f1f1f1;
    }

    .no-orders {
      text-align: center;
      color: #666;
      margin-top: 20px;
    }

    button {
      background: #dc3545;
      color: white;
      border: none;
      padding: 8px 16px;
      border-radius: 6px;
      cursor: pointer;
      margin-top: 20px;
    }

    button:hover {
      background: #b52a36;
    }

    .small {
      font-size: 0.9em;
      color: #555;
    }
  </style>
</head>
<body>
  <h1>📊 Dashboard – Mijn Webshop</h1>
  <div id="content"></div>

  <script>
    // === TOEGANGSCONTROLE ===
    const ADMIN_WACHTWOORD = "beheer123"; // ← pas aan naar jouw wachtwoord
    const wachtwoord = prompt("Voer admin-wachtwoord in:");
    if (wachtwoord !== ADMIN_WACHTWOORD) {
      alert("❌ Toegang geweigerd");
      window.location.href = "index.html";
    } else {
      laadDashboard();
    }

    // === DASHBOARD FUNCTIES ===
    function laadDashboard() {
      const content = document.getElementById("content");
      const bestellingen = JSON.parse(localStorage.getItem("orders")) || [];

      if (bestellingen.length === 0) {
        content.innerHTML = "<p class='no-orders'>Geen bestellingen gevonden.</p>";
        return;
      }

      let html = `
        <table>
          <tr>
            <th>Bestelnummer</th>
            <th>Klant</th>
            <th>Email</th>
            <th>GSM</th>
            <th>Adres</th>
            <th>Producten</th>
            <th>Totaal (€)</th>
            <th>Datum</th>
          </tr>
      `;

      bestellingen.forEach(b => {
        const klant = b.klant;
        html += `
          <tr>
            <td>${b.bestelnummer}</td>
            <td>${klant.voornaam} ${klant.naam}</td>
            <td>${klant.email}</td>
            <td>${klant.gsm}</td>
            <td>${klant.straat} ${klant.nummer}, ${klant.postcode} ${klant.gemeente}</td>
            <td>${b.producten.join(", ")}</td>
            <td>${b.totaal}</td>
            <td class="small">${b.datum}</td>
          </tr>
        `;
      });

      html += `</table>
        <button onclick="wisBestellingen()">🗑️ Wis alle bestellingen</button>
      `;

      content.innerHTML = html;
    }

    function wisBestellingen() {
      if (confirm("Weet je zeker dat je alle bestellingen wil verwijderen?")) {
        localStorage.removeItem("orders");
        alert("Alle bestellingen zijn verwijderd.");
        location.reload();
      }
    }
  </script>
</body>
</html>
